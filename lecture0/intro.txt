what is PHP?
PHP is a server scripting language, and a powerful tool for making dynamic and interactive Web pages.

PHP is a widely-used, free, and efficient alternative to competitors such as Microsoft's ASP.

PHP is an acronym for "PHP: Hypertext Preprocessor"
->Hypertext: Refers to text or content that contains links to other documents or resources, 
which is the basis of web pages (HTML).
->Preprocessor: PHP processes the server-side code before it's sent to the browser. 
This means it dynamically generates HTML, executes scripts, and interacts with databases, 
ensuring the user gets the final output.

PHP is a widely-used, open source scripting language
PHP scripts are executed on the server
PHP is free to download and use

ex. It is powerful enough to be at the core of the biggest blogging system on the web (WordPress)!

What is a PHP File?
PHP files can contain text, HTML, CSS, JavaScript, and PHP code
PHP code is executed on the server, and the result is returned to the browser as plain HTML
PHP files have extension ".php"

What Can PHP Do?
PHP can generate dynamic page content
PHP can create, open, read, write, delete, and close files on the server
PHP can collect form data
PHP can send and receive cookies
PHP can add, delete, modify data in your database
PHP can be used to control user-access
PHP can encrypt data

Why PHP?
PHP runs on various platforms (Windows, Linux, Unix, Mac OS X, etc.)
PHP is compatible with almost all servers used today (Apache, IIS, etc.)
PHP supports a wide range of databases
PHP is free. Download it from the official PHP resource: www.php.net
PHP is easy to learn and runs efficiently on the server side